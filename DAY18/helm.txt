Helm in k8s
===========
Helm is a package manager for Kubernetes applications that helps you define, install, and manage Kubernetes applications similar to yum/apt for linux distributions. 
It uses a concept called "charts" to package all the resources(pod,rs,deploy,pv,pvc.....) needed for an application into a single unit. Here’s a detailed breakdown of Helm and its components:

1. Key Concepts
---------------
Charts: A Helm chart is a collection of files that describe a related set of Kubernetes resources. 
Each chart contains templates for Kubernetes manifests, a Chart.yaml file (metadata), and optionally a values.yaml file for configuration.

Releases: A release is an instance of a chart running in a Kubernetes cluster. 
When you install a chart, you create a release. Each release can have multiple versions, and you can upgrade or roll back to previous versions.

Repository: Helm charts can be stored in repositories, which are collections of charts. 
You can create your own chart repositories or use public ones like the official Helm Hub or Artifact Hub.


2. Architecture
----------------
Helm operates in a client-server model:

helm 2:

https://www.google.com/search?q=helm+in+k8s+2&sca_esv=9c1db43462cf1df4&rlz=1C1UEAD_enIN1101IN1102&udm=2&biw=1440&bih=825&sxsrf=ADLYWIK4Xt_7ap01lm4VoujMDzvZC5TY6Q%3A1728902469006&ei=RfUMZ5ILsZ7j4Q__5ILIAg&ved=0ahUKEwjSwZDM142JAxUxzzgGHX-yACkQ4dUDCBA&uact=5&oq=helm+in+k8s+2&gs_lp=Egxnd3Mtd2l6LXNlcnAiDWhlbG0gaW4gazhzIDJIpQ5Q7AJYkgxwAXgAkAEAmAFhoAGRBaoBATe4AQPIAQD4AQGYAgWgAq8DwgIKEAAYgAQYQxiKBcICBRAAGIAEwgIEECMYJ8ICBhAAGAcYHsICCBAAGAUYBxgewgIIEAAYBxgIGB7CAgYQABgFGB7CAgYQABgIGB6YAwCIBgGSBwMyLjOgB_wd&sclient=gws-wiz-serp#vhid=CnN5ezLxGwlb8M&vssid=mosaic

helm 3:

https://www.google.com/search?q=helm+in+k8s+3+archetecture&sca_esv=9c1db43462cf1df4&rlz=1C1UEAD_enIN1101IN1102&udm=2&biw=1440&bih=825&sxsrf=ADLYWILeu84H6o3S6g8w1f-ITRaOqfA84Q%3A1728902522703&ei=evUMZ9jNKsKW4-EPmq6auQs&ved=0ahUKEwjY8t3l142JAxVCyzgGHRqXJrcQ4dUDCBA&uact=5&oq=helm+in+k8s+3+archetecture&gs_lp=Egxnd3Mtd2l6LXNlcnAiGmhlbG0gaW4gazhzIDMgYXJjaGV0ZWN0dXJlSJ8lUOEBWPojcAF4AJABAJgB0QGgAZkMqgEFNS43LjG4AQPIAQD4AQGYAgCgAgCYAwCIBgGSBwCgB8kE&sclient=gws-wiz-serp#vhid=HShMz9xvTEjAwM&vssid=mosaic


3. Installation
----------------

curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3

chmod 700 get_helm.sh

sh get_helm.sh

4. How to unistall helm
------------------------

sudo rm /usr/local/bin/helm

rm -rf ~/.config/helm

rm get_helm.sh

5. Chart Structure
------------------

A typical Helm chart structure looks like this:

jio-stg-om-chart/
  ├── Chart.yaml        # Metadata about the chart
  ├── values.yaml       # Default configuration values for templates
  ├── charts/           # Subcharts (if any)
  └── templates/        # Template files for Kubernetes resources
      ├── deployment.yaml   
      ├── service.yaml
      └── pv.yaml





6. Templating
--------------
Helm uses Go templates to generate Kubernetes manifests dynamically. You can use placeholders in your templates, which get replaced by values from values.yaml or from command-line parameters when deploying.


7. Values.yaml 
---------------

You can customize deployments by creating your own values.yaml file, overriding default values in the chart:

stgvalues.yaml
----
replicaCount: 2
image:
  repository: my-repo/my-app
  tag: "1.0.0"
service:
  type: LoadBalancer
  port: 80
request:
 cpu: 1
 memory: 12Gi

prod-values.yaml
-----

replicaCount: 5
image:
  repository: my-repo/my-app-prod
  tag: "1.1.0"
service:
  type: LoadBalancer
  port: 80
request:
 cpu: 8
 memory: 128Gi




8. Best Practices
-----------------

Version Control: Store your charts in a version control system.
Reusable Charts: Create charts that can be reused across different environments.
Testing: Use the helm test command to verify chart functionality.
Security: Keep your charts and values secure, especially sensitive data.

9. Common Charts
-----------------
Many common applications have Helm charts available, including:

NGINX
Prometheus
Grafana
MySQL
PostgreSQL
metrics-server
jenkins

You can find these charts in public repositories like the Helm Hub.





===============================================


How to deploy third party applications like metrics server, prometheus, etc ?

EX: metrics server

kubectl config current-context  --> to check the correct environment 

kubectl top nodes
kubectl top pods


url: https://artifacthub.io/

search metrics-server

url: https://artifacthub.io/packages/helm/metrics-server/metrics-server

How to check helm repo added or not?

helm repo ls

How to add helm repo?

helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server/




helm repo ls

How to search chart for that repo?

helm search repo metrics-server

How to see the default values of that chart?

helm show values metrics-server/metrics-server

How to see the template(all yaml files) ?

helm template metrics-server/metrics-server



----------------------

How to install/deploy the chart?

helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns --dry-run

helm upgrade --install metrics-server metrics-server/metrics-server --set replicas=2 -n test-ns --dry-run

helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns --dry-run

helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns

helm ls -A

helm ls -n kube-system



------

increased to 2 replicas???
--------------------------

helm upgrade --install metrics-server metrics-server/metrics-server --set replicas=3

How to rollback?
-----------------

helm ls

helm rollback metrics-server

How to uninsatll?
----------------

helm uninstall metrics-server



================================

NOTE: If you want to pass more values through --set is not a good practise, so we need to use values.yaml


helm show values metrics-server/metrics-server

vi metricservervalues.yaml

resources:
  requests:
    cpu: 300m
    memory: 512Mi
replicas: 2


helm upgrade --install metrics-server -f metricservervalues.yaml metrics-server/metrics-server

kubectl describe po <pod-name> --> to see the updated values


--> create a production cluster separate values.yaml and deploy


====================================




--> To deploy your Kubernetes resources as a Helm chart, you'll need to create a basic Helm chart structure. 
Here’s a step-by-step guide to help you set up your Helm chart with the provided Deployment and Service configurations.


Step 1: Create a Helm Chart
-------
First, create a new Helm chart:

helm create javawebapp

This will create a directory structure like this:

javawebapp/
├── Chart.yaml
├── templates/
│   ├── deployment.yaml
│   ├── service.yaml
│   └── ...
└── values.yaml






Step 2: Modify the Chart.yaml


Edit the Chart.yaml file to provide metadata for your chart. Here’s an example:

apiVersion: v2
name: javawebapp
description: A Helm chart for deploying a Java web application
version: 1.0.0
appVersion: "1.0.1"


Step 3: Update the values.yaml
------
In values.yaml, set default values that can be customized when deploying. For example:

replicaCount: 2

image:
  repository: kkeducation123456/java-web-app
  pullPolicy: IfNotPresent
  tag: "1.0.1"

service:
  type: NodePort
  port: 80
  targetPort: 8080


Step 4: Edit the Deployment Template
-------


In templates/deployment.yaml, replace its content with your Deployment configuration, making use of Helm templates:

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
      - name: {{ .Release.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: {{ .Values.service.targetPort }}


Step 5: Edit the Service Template
-------

In templates/service.yaml, modify it to use values from values.yaml:

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}


Step 6: Deploy the Helm Chart
-------

Now that you have created your Helm chart, you can install it using the following command:

helm install <release-name> ./javawebapp

helm upgrade --install javawebapp ./javawebapp  --dry-run

helm upgrade --install javawebapp ./javawebapp

========



helm create <chartName>

helm lint <chartName> --> for validation

helm package javawebapp  --> to create a tar file

helm upgrade --install javawebapp2 javawebapp-1.0.0.tgz -n prod

helm show values javawebapp-1.0.0.tgz

helm status <resourceName> --show-resources


helm uninstall mavenwebapp -n <NS>









NOTES EXPLANATION BY CHATGPT:
-----------------------------

1. What is Helm (core idea)
Helm = package manager for Kubernetes
Linux → apt / yum
Kubernetes → Helm
Why we need Helm
Without Helm:

Write multiple YAML files
Manage versions manually
Hard to reuse
With Helm:
One chart → deploy complete application ✔

2. Key Concepts
Charts
A chart is a package of Kubernetes resources
Example:
Deployment + Service + PV + PVC → bundled together
Releases
When you install a chart:
Chart → becomes → Release
Example:
helm install myapp ./chart
myapp = release name
Repository
Storage for charts
Examples:
ArtifactHub
Your own repo

3. Helm Architecture
Helm 2 (OLD)
Had Tiller (server component)
Security issues
Helm 3 (CURRENT ✔)
No server
Only client (helm CLI)
Simpler and secure

4. Installation
Your steps:
curl -fsSL -o get_helm.sh ...
chmod 700 get_helm.sh
sh get_helm.sh
Installs Helm CLI

5. Chart Structure (VERY IMPORTANT)
jio-stg-om-chart/
  ├── Chart.yaml
  ├── values.yaml
  ├── charts/
  └── templates/
Meaning
Chart.yaml
Metadata
Name, version, description
values.yaml
Default configuration
All customizable values stored here
templates/
Actual Kubernetes YAML files
BUT with:

Dynamic placeholders (templates)

6. Templating (MOST IMPORTANT CONCEPT)
Example from your Deployment
replicas: {{ .Values.replicaCount }}
Meaning
Take value from values.yaml → replace here
Example
values.yaml:
replicaCount: 2
Final generated YAML:
replicas: 2

7. values.yaml (environment-specific configs)
Your examples
stgvalues.yaml
replicaCount: 2
image:
  repository: my-repo/my-app
  tag: "1.0.0"
prod-values.yaml
replicaCount: 5
image:
  repository: my-repo/my-app-prod
  tag: "1.1.0"
Meaning
Same chart → different environments → different values

8. Helm Commands (VERY IMPORTANT)
Add repo
helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server/
Register external charts

Search chart
helm search repo metrics-server

View default values
helm show values metrics-server/metrics-server

View templates
helm template metrics-server/metrics-server
Shows generated YAML without deploying

9. Install / Deploy chart
Dry run
helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns --dry-run
Test only (no real deployment)
Actual deploy
helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns
Why upgrade --install?
If exists → upgrade
If not → install
10. Override values
Using --set
helm upgrade --install metrics-server metrics-server/metrics-server --set replicas=3
Not recommended for large configs
Use values file instead:
helm upgrade --install metrics-server -f metricservervalues.yaml metrics-server/metrics-server

11. Rollback
helm rollback metrics-server
Reverts to previous version

12. Uninstall
helm uninstall metrics-server
Deletes release




Installing third-party apps using Helm (like metrics-server)
Creating your own Helm chart (javawebapp)
Let’s separate them and walk step-by-step like a real workflow.
PART 1: Deploying third-party apps (metrics-server)
This is the most important real-world use case.
Goal
Install metrics-server so you can run:
kubectl top nodes
kubectl top pods
Step 1: Check cluster
kubectl config current-context
Make sure you are in the correct cluster
Step 2: Add Helm repository
Charts are stored in repos
helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server/
Verify
helm repo ls
Step 3: Search chart
helm search repo metrics-server
Shows available chart
Step 4: Inspect before installing (VERY IMPORTANT)
View default values
helm show values metrics-server/metrics-server
This shows:
All configurable options
View generated YAML
helm template metrics-server/metrics-server
Shows:
Deployment + Service + RBAC etc
Step 5: Dry run (safe test)
helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns --dry-run
Nothing is deployed yet
Step 6: Actual install
helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns
Why upgrade --install?
If exists → upgrade
If not → install
Step 7: Verify
helm ls -A
kubectl get pods -n test-ns
Now test
kubectl top nodes
Changing values (replicas example)
Using --set
helm upgrade --install metrics-server metrics-server/metrics-server --set replicas=3
Problem
--set is messy for large configs ❌
Better approach: values file
Create file
vi metricservervalues.yaml
Your file (same as notes)
resources:
  requests:
    cpu: 300m
    memory: 512Mi
replicas: 2
Apply it
helm upgrade --install metrics-server -f metricservervalues.yaml metrics-server/metrics-server
Rollback
helm rollback metrics-server
Goes to previous version
Uninstall
helm uninstall metrics-server
Summary of Part 1
Add repo → Search → Inspect → Dry-run → Install → Customize → Upgrade

PART 2: Creating your own Helm chart
Now this is completely different.
Here YOU are building a chart.
Step 1: Create chart
helm create javawebapp
Creates:
javawebapp/
  Chart.yaml
  values.yaml
  templates/
Step 2: Chart.yaml (metadata)
Your file:
apiVersion: v2
name: javawebapp
description: A Helm chart for deploying a Java web application
version: 1.0.0
appVersion: "1.0.1"
Just info, nothing dynamic
Step 3: values.yaml (CONFIG FILE)
replicaCount: 2
image:
  repository: kkeducation123456/java-web-app
  tag: "1.0.1"
service:
  type: NodePort
  port: 80
  targetPort: 8080
This controls everything
Step 4: Deployment template (IMPORTANT)
Your YAML:
replicas: {{ .Values.replicaCount }}
What happens?
Helm replaces:
{{ .Values.replicaCount }} → 2
Another example
image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
becomes:
kkeducation123456/java-web-app:1.0.1
Step 5: Service template
name: {{ .Release.Name }}-service
If release name = javawebapp
Service name = javawebapp-service
Step 6: Deploy your chart
helm install javawebapp ./javawebapp
OR
helm upgrade --install javawebapp ./javawebapp
Step 7: Validate
helm lint javawebapp
Step 8: Package
helm package javawebapp
Creates:
javawebapp-1.0.0.tgz
Step 9: Install packaged chart
helm upgrade --install javawebapp2 javawebapp-1.0.0.tgz -n prod
Step 10: Check status
helm status javawebapp
Final mental model (VERY IMPORTANT)
Third-party app flow
Helm repo → Chart → Install → Customize
Your own app flow
Create chart → Write templates → Use values → Deploy
Final summary
You were confused because:
First part = using Helm
Second part = building Helm charts
One-line memory
Helm = install apps + build reusable templates











Helm Chart Structure (your example)
jio-stg-om-chart/
  ├── Chart.yaml
  ├── values.yaml
  ├── charts/
  └── templates/
1. Chart.yaml (Metadata file)
What it is
This is the identity card of your chart
Example content
apiVersion: v2
name: jio-stg-om-chart
description: Helm chart for Jio staging OM app
version: 1.0.0
appVersion: "1.0.0"
Fields explained
apiVersion → Helm version (always v2 now)
name → chart name
version → chart version (important for upgrades)
appVersion → application version
description → optional
Why it exists
Helm uses this to identify and manage your chart

2. values.yaml (MOST IMPORTANT)
What it is
Default configuration for your chart
Example content
replicaCount: 2
image:
  repository: myrepo/myapp
  tag: "1.0.0"

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 200m
    memory: 256Mi
Why it exists
You define values here → templates use them
Flow
values.yaml → passed into → templates → generates final YAML
Key idea
Change values.yaml → no need to change templates

3. templates/ (Actual Kubernetes YAMLs)
What it is
This contains all Kubernetes resources
BUT with dynamic placeholders
Example files inside
templates/
  deployment.yaml
  service.yaml
  ingress.yaml
  pvc.yaml
Example content (deployment.yaml)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  template:
    spec:
      containers:
      - name: app
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
Important concept
{{ }} → Helm template syntax
What happens
Helm replaces:
{{ .Values.replicaCount }} → 2
Final output (after Helm)
replicas: 2

4. charts/ (Subcharts)
What it is
Used for dependencies
Example
charts/
  mysql/
  redis/
Meaning
Your app depends on:
Main app + MySQL + Redis
Example use case
Your app needs DB → include MySQL chart
How it's defined (in Chart.yaml)
dependencies:
- name: mysql
  version: 1.6.0
  repository: https://charts.bitnami.com/bitnami
Full Flow (VERY IMPORTANT)
Step-by-step
1. values.yaml → defines values
2. templates/ → uses values with {{ }}
3. Helm processes templates
4. Final Kubernetes YAML is generated
5. Resources are created in cluster
Real-world example
values.yaml
replicaCount: 3
deployment.yaml
replicas: {{ .Values.replicaCount }}
Final output
replicas: 3
Quick comparison
File	Purpose
Chart.yaml	Metadata
values.yaml	Configuration
templates/	Kubernetes YAML
charts/	Dependencies
One-line memory trick
Chart.yaml → who am I
values.yaml → what values I use
templates/ → what I create
charts/ → what I depend on
Final summary
Helm chart = combination of:
Metadata (Chart.yaml)
Config (values.yaml)
Templates (templates/)
Dependencies (charts/)








