1. Why do we need ConfigMaps & Secrets?
Imagine your app needs:
DB host → mongosvc
DB username → devdb
DB password → devdb@123
Hardcoding inside image → bad practice
Editing code for every environment → messy
Kubernetes solution:
Configuration is stored OUTSIDE the container

2. What is a ConfigMap?
A ConfigMap is:
A Kubernetes object used to store non-sensitive configuration data as key-value pairs
Example use cases
App settings
URLs
Feature flags
Port numbers
File-based configs
Example ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  DB_HOST: mongosvc
  DB_PORT: "27017"
  APP_MODE: production

3. What is a Secret?
A Secret is:
A Kubernetes object used to store sensitive data
Example use cases
Passwords
API keys
Tokens
Certificates
Example Secret
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
data:
  DB_USERNAME: ZGV2ZGI=        # base64(devdb)
  DB_PASSWORD: ZGV2ZGJAMTIz    # base64(devdb@123)

Note:
Values must be base64 encoded

4. Key difference (VERY IMPORTANT)
Feature	ConfigMap	Secret
Data type	Non-sensitive	Sensitive
Encoding	Plain text	Base64
Security	Less secure	More secure
Use case	Config	Credentials

5. How to USE them in Pods
You can use both in 2 ways:
Method 1: As Environment Variables
Using ConfigMap
env:
- name: DB_HOST
  valueFrom:
    configMapKeyRef:
      name: app-config
      key: DB_HOST
Using Secret
env:
- name: DB_PASSWORD
  valueFrom:
    secretKeyRef:
      name: app-secret
      key: DB_PASSWORD
Shortcut (import all values)
envFrom:
- configMapRef:
    name: app-config
- secretRef:
    name: app-secret

6. Method 2: As Files (VERY POWERFUL)
ConfigMap as volume
volumes:
- name: config-vol
  configMap:
    name: app-config
Mount in container
volumeMounts:
- name: config-vol
  mountPath: /etc/config

Inside container:

/etc/config/DB_HOST
/etc/config/APP_MODE
Secret as volume
volumes:
- name: secret-vol
  secret:
    secretName: app-secret

7. Full real-world example (your style)
containers:
- name: springapp
  image: myapp

  envFrom:
  - configMapRef:
      name: app-config
  - secretRef:
      name: app-secret

Your app automatically gets:

DB_HOST
DB_PORT
DB_USERNAME
DB_PASSWORD

8. How this fits with your MongoDB example
Instead of:
env:
- name: MONGO_DB_USERNAME
  value: devdb
Better:
env:
- name: MONGO_DB_USERNAME
  valueFrom:
    secretKeyRef:
      name: app-secret
      key: DB_USERNAME

9. Important rules
ConfigMap
Use for non-sensitive data
Easy to edit without rebuilding image
Secret
Use for passwords & credentials
Base64 encoded (not encrypted by default )

10. Common mistakes
Storing passwords in ConfigMap
Hardcoding secrets in YAML
Forgetting base64 encoding
Mixing up key names

11. Simple mental model
ConfigMap = App settings
Secret = App passwords
Final summary
ConfigMap → non-sensitive config
Secret → sensitive data
Both can be used as:
Environment variables
Mounted files (volumes)
