


RBAC
====


RBAC (Role-Based Access Control) in Kubernetes is a powerful method for regulating access to the Kubernetes API based on the roles of individual users or groups. 
Here's a breakdown of its key components and how it works:

Key Components

Role: Defines a set of permissions within a specific namespace. It grants access to resources like pods, deployments, etc.

RoleBinding: Grants the permissions defined in a Role to a user or a group within a specific namespace.

ClusterRole: Similar to a Role but applies across the entire cluster. It can be used for non-namespaced resources or for cluster-wide permissions.


ClusterRoleBinding: Grants the permissions defined in a ClusterRole to a user or a group across the entire cluster.


Practical
=========

step 1: install kubectl in your system(windows)
------

link: https://kubernetes.io/docs/tasks/tools/install-kubectl-windows/

curl.exe -LO "https://dl.k8s.io/release/v1.31.0/bin/windows/amd64/kubectl.exe"

      or

curl.exe -LO "https://dl.k8s.io/release/v1.32.0/bin/windows/amd64/kubectl.exe"

     or 


curl.exe -LO "https://dl.k8s.io/release/v1.33.0/bin/windows/amd64/kubectl.exe"


     or
curl.exe -LO "https://dl.k8s.io/release/v1.34.0/bin/windows/amd64/kubectl.exe"



add the environment details.

C:\Users\gpras\OneDrive\Desktop\rbac-practise  --> add env path

step 2: Install aws cli
-------

link: https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html

Download and run the AWS CLI MSI installer for Windows (64-bit):

https://awscli.amazonaws.com/AWSCLIV2.msi





step 3: create IAM user and create a access and seceret key for that user
------


https://gobidesi.signin.aws.amazon.com/console

uname:   kkfunda


pwd:    Vision@2031


AK: AK

SK: lg







step 4: we need aws permissions
-------

aws configure

access key: 
secret key:
region: ap-south-1
format: table

aws sts get-caller-identity

aws eks list-clusters
aws eks update-kubeconfig --name my-demo-cluster --region ap-south-1


step 5: create eks read-only policy and attach to the user
-------

aws sts get-caller-identity

aws eks list-clusters
aws eks update-kubeconfig --name my-demo-cluster --region ap-south-1

cat /c/Users/gpras/.kube/config

But we are not able to list kubectl get po, etc



step 6: connect to linux machine where we have k8s admin cli access.
-------

kubectl get cm -n kube-system

cm: aws-auth

NOTE: add users

kubectl edit cm aws-auth -n kube-system


apiVersion: v1
data:
  mapRoles: |
    - groups:
      - system:bootstrappers
      - system:nodes
      rolearn: arn:aws:iam::418295704475:role/EKS-worker-role-class
      username: system:node:{{EC2PrivateDNSName}}
  mapUsers: |
    - userarn: arn:aws:iam::418295704475:user/prasanth
      username: prasanth
kind: ConfigMap
metadata:
  creationTimestamp: "2024-10-11T07:12:50Z"
  name: aws-auth
  namespace: kube-system
  resourceVersion: "52395"
  uid: fe764e06-c2ec-4f84-bc3b-5732c1ebfaeb



kubectl get pods --> we dont have permission


srihari --> RBAC

sai --> admin


=================================

--> Now create Role and RoleBinding

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: default
  name: pod-reader
rules:
- apiGroups: [""] # "" indicates the core API group
  resources: ["pods"]
  verbs: ["get", "watch", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: read-pods
  namespace: prod
subjects:
- kind: User
  name: kkfunda1
  apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io




=========


apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: fullaccesscr
rules:
- apiGroups: ["*"]
  resources: ["*"]
  verbs: ["*"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: fullaccess
subjects:
- kind: User
  name: kkfunda
  apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: fullaccesscr
  apiGroup: rbac.authorization.k8s.io












NOTES EXPLANATION BY CHATGPT:
-----------------------------


You’ve put together a full RBAC + EKS access flow, which is great—but it mixes a few layers (AWS IAM vs Kubernetes RBAC). 
I’ll untangle it and explain clearly what each step is doing and why.

1. Big Picture (VERY IMPORTANT)
There are 2 levels of access control here:
1. AWS IAM → Can you connect to EKS cluster?
2. Kubernetes RBAC → What can you do inside cluster?
Flow
IAM User → aws-auth ConfigMap → Kubernetes User → RBAC → Permissions

2. Step-by-step explanation of your notes
Step 1 & 2: Install tools
kubectl → talk to Kubernetes
aws cli → talk to AWS
These are just tools (no permissions yet)
Step 3: Create IAM user
You created:
Access Key
Secret Key
This is your AWS identity
Step 4: Configure AWS CLI
aws configure
Stores credentials locally
Important commands
aws sts get-caller-identity
Confirms:
Who am I in AWS?
aws eks update-kubeconfig --name my-demo-cluster
This does:
Connect kubectl → EKS cluster
Important problem you saw
kubectl get pods → NOT working 
Why?
Because:
IAM access ≠ Kubernetes access
You can connect to cluster but:
You are NOT authorized inside Kubernetes
Step 5 & 6: aws-auth ConfigMap (VERY IMPORTANT)
What is aws-auth?
Special ConfigMap in EKS:
Maps IAM users → Kubernetes users/groups
Your example:
mapUsers: |
  - userarn: arn:aws:iam::418295704475:user/prasanth
    username: prasanth
Meaning
IAM user (prasanth)
→ becomes Kubernetes user (prasanth)
But still no access?
Yes, because:
User is identified, but has NO permissions yet

3. Now RBAC comes into picture
Core RBAC components
Role
kind: Role
Defines:
What actions are allowed (within namespace)
RoleBinding
kind: RoleBinding
Connects:
User → Role
ClusterRole
kind: ClusterRole
Same as Role but:
Cluster-wide permissions
ClusterRoleBinding
Connects:
User → ClusterRole (cluster-wide)

4. Your Role example explained
kind: Role
namespace: default
name: pod-reader
Rules
resources: ["pods"]
verbs: ["get", "watch", "list"]
Meaning:
User can:
- list pods
- get pod details
- watch pods
RoleBinding
kind: RoleBinding
namespace: prod
Subject
name: kkfunda1
This is:
Kubernetes user (mapped via aws-auth)
RoleRef
name: pod-reader
Meaning:
Give pod-reader permissions to this user
Important mistake in your YAML
Role → namespace: default
RoleBinding → namespace: prod
This will NOT work 
Correct rule
Role and RoleBinding must be in SAME namespace

5. ClusterRole example (full access)
kind: ClusterRole
Rules
apiGroups: ["*"]
resources: ["*"]
verbs: ["*"]
Meaning:
FULL ACCESS (admin)
ClusterRoleBinding
kind: ClusterRoleBinding
Subject
name: kkfunda
RoleRef
name: fullaccesscr
Meaning
User kkfunda → gets full cluster admin access

6. Complete flow (VERY IMPORTANT)
Step-by-step:
1. IAM user created
2. Added in aws-auth ConfigMap
3. IAM user → mapped to Kubernetes user
4. RBAC applied (Role/ClusterRole)
5. User gets permissions

7. Why kubectl didn’t work initially
Because:
IAM user → recognized
BUT → no RBAC permissions 

8. Real-world analogy
IAM → Entry pass to building
RBAC → What rooms you can access

9. Common mistakes (important)
Forgetting aws-auth mapping
Role and RoleBinding namespace mismatch
Wrong username in RoleBinding
Giving full access unnecessarily
Confusing IAM vs RBAC

10. One-line memory
IAM = who you are
RBAC = what you can do
Final summary
Your setup does:
IAM user → connects to EKS
aws-auth → maps user to Kubernetes
RBAC → gives permissions







RBAC BY CHATGPT:
--------------

1. What is RBAC?
RBAC (Role-Based Access Control) in Kubernetes is:
A system to control who can do what on which resources
One-line idea
RBAC = Authorization system of Kubernetes

2. Big Picture (VERY IMPORTANT)
User → (authenticated) → Kubernetes
        ↓
     RBAC decides
        ↓
Allow / Deny

3. What RBAC controls
RBAC controls access to:
Pods
Deployments
Services
ConfigMaps
Secrets
Nodes
etc.

4. Core components (4 things you must know)
1. Role
Defines permissions
"What actions are allowed?"
2. RoleBinding
Assigns Role to a user
"Who gets those permissions?"
3. ClusterRole
Same as Role but:
Cluster-wide permissions
4. ClusterRoleBinding
Assigns ClusterRole to user
5. Visual understanding
User → RoleBinding → Role → Permissions
OR
User → ClusterRoleBinding → ClusterRole → Permissions

6. Example (simple and powerful)
Goal
User can read pods in prod namespace only
Step 1: Role
kind: Role
namespace: prod
rules:
- resources: ["pods"]
  verbs: ["get", "list"]
Defines:
Read pods only
Step 2: RoleBinding
kind: RoleBinding
namespace: prod
subjects:
- name: user1
roleRef:
  name: pod-reader
Assigns:
user1 → pod-reader role
Result
user1 → can read pods in prod namespace only

7. Role vs ClusterRole (IMPORTANT)
Feature	Role	ClusterRole
Scope	Namespace	Entire cluster
Use case	App-level access	Admin / cluster access
Example
Role
Access pods in "prod" namespace
ClusterRole
Access nodes (cluster-wide)

8. How RBAC works internally
Step-by-step
1. User sends request (kubectl get pods)
2. Kubernetes checks:
   → Who is user?
3. RBAC checks:
   → Is there a Role/ClusterRole?
   → Is it bound to user?
4. Decision:
   → Allow ✔ or Deny ❌

9. Real-world analogy
Office building:
User = Employee
Role = Job role (Developer, Manager)
Binding = HR assigning role
RBAC = Security system controlling access

10. Important concepts
Verbs (actions)
get, list, watch → read
create, delete → write
→ all actions
Resources
pods, deployments, services, secrets
apiGroups
"" → core (pods, services)
apps → deployments
11. Common RBAC patterns
Read-only user
Can only view resources
Dev user
Can create pods/deployments
Admin user
Full access

12. Common mistakes
Role and RoleBinding in different namespaces
Wrong username (must match aws-auth mapping)
Giving full access unnecessarily
Forgetting apiGroups

13. One-line memory
RBAC = Who can do what in Kubernetes
Final summary
RBAC controls permissions inside Kubernetes
4 main components:
Role
RoleBinding
ClusterRole
ClusterRoleBinding
Flow:
User → Binding → Role → Permission





