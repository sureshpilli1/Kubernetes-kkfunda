# **DaemonSet - Complete Guide KK FUNDA**

## **1. What is a DaemonSet?**

A **DaemonSet** ensures that **one copy of a Pod runs on every node** in the Kubernetes cluster.

So whenever:

* A new node joins → Pod is automatically created on that node.
* A node is removed → Pod is automatically removed.

###  Why this is needed?

Some workloads are **node-specific** and must run everywhere, such as:

| Use Case                                             | Purpose                        |
| ---------------------------------------------------- | ------------------------------ |
| **Log collectors** (Fluentd, Filebeat)               | Collect logs from each node    |
| **Monitoring agents** (Node Exporter, Datadog agent) | Collect node-level metrics     |
| **Security agents**                                  | Enforce policies on each node  |
| **Kube-proxy / network plugins**                     | Manage networking on each node |

---

## **2. How DaemonSet Works Internally**

| Component                     | Responsibility                                    |
| ----------------------------- | ------------------------------------------------- |
| `DaemonSet Controller`        | Watches nodes and ensures Pods exist on all nodes |
| `Node Selector / Tolerations` | Controls where Pods can run                       |
| `Template.spec`               | Defines the container specification               |

DaemonSet is **similar to Deployment**, except:

* **Deployment** tries to maintain a fixed replica count (e.g., 3 Pods).
* **DaemonSet** runs **1 Pod per node** (not fixed count).

---

## **3. Real-Time Example: Node Exporter (Monitoring Agent)**

### **Objective**

Deploy **node-exporter** on **every node** to collect CPU, RAM, Disk metrics.

### **YAML File: `node-exporter-daemonset.yaml`**

```yaml
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: node-exporter
  namespace: kube-system
spec:
  selector:
    matchLabels:
      app: node-exporter
  template:
    metadata:
      labels:
        app: node-exporter
    spec:
      hostNetwork: true
      hostPID: true
      containers:
      - name: node-exporter
        image: prom/node-exporter:latest
        ports:
        - containerPort: 9100
          name: metrics
```

### **Key Points**

| Field                 | Meaning                                                          |
| --------------------- | ---------------------------------------------------------------- |
| `hostNetwork: true`   | The Pod shares the node network → metrics accessible via node IP |
| `hostPID: true`       | Allows access to host process namespace                          |
| `containerPort: 9100` | Port where metrics are exposed                                   |

---

## **4. Apply the DaemonSet**

```bash
kubectl apply -f node-exporter-daemonset.yaml
```

---

## **5. Validate DaemonSet**

### **Check DaemonSet**

```bash
kubectl get daemonset -n kube-system
```

Expected Output:

```
NAME            DESIRED   CURRENT   READY
node-exporter   3         3         3
```

> `DESIRED = number of nodes in your cluster`

---

### **List Pods running on each node**

```bash
kubectl get pods -n kube-system -o wide | grep node-exporter
```

Example Output:

```
node-exporter-abcde   Running   ip:10.0.1.10   node:worker1
node-exporter-fghij   Running   ip:10.0.1.11   node:worker2
node-exporter-klmno   Running   ip:10.0.1.12   node:master
```

---

## **6. Verify Node Metrics (Important Step)**

Pick any node IP & test:

```bash
curl http://<NODE-IP>:9100/metrics
```

Example:

```bash
curl http://192.168.1.101:9100/metrics
```

You should see output like:

```
# HELP node_cpu_seconds_total Seconds the CPUs spent in each mode.
node_cpu_seconds_total{cpu="0",mode="idle"} 15052.92
```

###  If this output appears → **DaemonSet is working successfully**

---

## **7. (Optional) Expose via Service (for Prometheus)**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: kube-system
spec:
  selector:
    app: node-exporter
  ports:
  - name: metrics
    port: 9100
    targetPort: 9100
```

Apply:

```bash
kubectl apply -f node-exporter-service.yaml
```

---

## **8. Troubleshooting**

| Issue                             | Reason                         | Fix                                                                |
| --------------------------------- | ------------------------------ | ------------------------------------------------------------------ |
| Pods running only on worker nodes | Schedulable master disabled    | `kubectl taint node master node-role.kubernetes.io/control-plane-` |
| Pod not starting                  | Image pull issue               | Check image URL / registry access                                  |
| Metrics not showing               | Firewall / firewallD blocking  | Allow port `9100` on each node                                     |
| Port conflicts                    | Something else using port 9100 | Change container port                                              |

---

## **9. When to Use Deployment Instead of DaemonSet**

Use **Deployment** when:

* The app is **not tied to nodes**
* You just need a **fixed number of replicas**
* You want **rolling updates / autoscaling**

Use **DaemonSet** when:

* Workload must run **on every node**
* Workload needs **host-level access









1️⃣ What is a DaemonSet?
A DaemonSet ensures:

✅ Exactly one Pod runs on every Node
OR
✅ One Pod runs on selected Nodes

Unlike a Deployment (which creates N replicas and spreads them),
DaemonSet = 1 Pod per Node automatically

2️⃣ Why Do We Need DaemonSet?
Some services must run on every Node, not just a few Pods.
Examples:
Use CaseWhy It Must Run on Every NodeMonitoring agentNeeds node-level metricsLog collectorLogs are stored locally on each nodekube-proxyHandles networking rules on each nodeCNI pluginProvides networking to Pods

3️⃣ Key Characteristics
✅ 1 Pod per Node
If you have:


1 Master


2 Workers


DaemonSet creates:


3 Pods total (1 on each node)


If a new Worker joins:


DaemonSet automatically creates 1 new Pod there.


If a Node is removed:


Its Pod is automatically deleted.


No manual scaling needed 🔥

4️⃣ From Your Screenshot (Real Example)
You ran:
kubectl get all -A -o wide

And saw system pods like:


coredns


etcd


kube-apiserver


kube-controller-manager


kube-scheduler


kube-proxy


weave-net


Let’s classify them 👇

5️⃣ Control Plane Pods (Master Only)
These run only on Master Node:


etcd


kube-apiserver


kube-controller-manager


kube-scheduler


Why?
Because these are control plane components.
They manage the cluster. They don’t need to run on worker nodes.
In your case:
Master:
ip-172-31-34-191

These pods run only there.

6️⃣ DaemonSet Pods (Run on All Nodes)
From your screenshot:
🔹 kube-proxy
🔹 weave-net (CNI plugin)
These are deployed as DaemonSets.
So you saw:


kube-proxy → 3 Pods


weave-net → 3 Pods


And they run on:


ip-172-31-34-191 (Master)


ip-172-31-5-194 (Worker 1)


ip-172-31-3-215 (Worker 2)


This proves:

DaemonSet = One Pod per Node


7️⃣ Why kube-proxy is DaemonSet?
kube-proxy manages:


Service networking rules


iptables rules


Routing between Pods


Since networking happens on each Node, it must run on each Node.

8️⃣ Why weave-net is DaemonSet?
Weave is a CNI plugin.
CNI provides:


Pod IP allocation


Pod-to-Pod communication


Overlay networking


Each Node needs networking capability → So 1 Pod per Node.

9️⃣ Difference: Deployment vs DaemonSet
FeatureDeploymentDaemonSetPods per NodeRandomExactly 1 per NodeScalingManual replicasAuto (based on node count)Use caseAppsSystem servicesNew node joinNo automatic podAutomatically creates Pod

🔟 Selective Node Deployment
DaemonSet doesn’t always have to run on all nodes.
You can control it using:


nodeSelector


nodeAffinity


taints & tolerations


Example:
nodeSelector:
  node-role.kubernetes.io/worker: ""

This runs DaemonSet only on worker nodes.

1️⃣1️⃣ Mental Model (Very Important 🧠)
Think like this:
Deployment → "Run 3 copies of my app somewhere"
DaemonSet → "I don’t care about replicas.
I want 1 copy on EVERY machine."

1️⃣2️⃣ Real Production Use Cases
Monitoring


Prometheus Node Exporter


Datadog Agent


Logging


Fluentd


Filebeat


Logstash


Networking


kube-proxy


Calico


Weave


Flannel



1️⃣3️⃣ What Happens If New Worker Joins?
Let’s say:
You had 3 nodes → 3 DaemonSet pods
You add 1 worker:
Immediately → 4th pod created automatically
That is automatic cluster-wide coverage.

1️⃣4️⃣ Interview-Level Explanation
If interviewer asks:

What is DaemonSet?

You say:
“A DaemonSet ensures that exactly one Pod runs on each Node in a Kubernetes cluster. It is mainly used for system-level services like logging agents, monitoring agents, and networking components that must run on every node.”
That’s a perfect answer.

1️⃣5️⃣ Short Summary
DaemonSet =
✔ One Pod per Node
✔ Auto-created on new nodes
✔ Used for system-level components
✔ No manual scaling
Deployment =
✔ N replicas
✔ Used for applications

5. Basic DaemonSet
Step 1: Create Basic DaemonSet

This example runs nginx on all worker Nodes (not master/control-plane).

You don’t set replicas manually; the Pod count always equals the Node count.

apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nginx
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80

kubectl apply -f ds.yaml

daemonset.apps/nginx created

Step 2: Check Cluster Nodes
kubectl get nodes


1 Master (ip-172-31-34-191) and 2 Workers (ip-172-31-5-194, ip-172-31-3-215).

Step 3: Verify Pods Created by DaemonSet
kubectl get all -o wide


This shows all resources in the current namespace with extra details like Pod IPs, Node names, and images.

Only 2 Pods running → one on each Worker Node.

No Pod on the Master (ip-172-31-34-191).

Step 4: Check Taints on Master vs Worker
kubectl describe node ip-172-31-34-191
kubectl describe node ip-172-31-5-194


Worker Nodes have no taints, so Pods are scheduled normally.

From Master node:

Taints: node-role.kubernetes.io/control-plane:NoSchedule


Master Node has NoSchedule taint, so DaemonSet Pods are not placed there by default.

By default, DaemonSet Pods do not run on master/control-plane Nodes because they have a NoSchedule taint.

6. Deploy DaemonSet on Master Node also

First delete old DS:

kubectl delete -f ds.yaml

daemonset.apps "nginx" deleted


Now apply updated DS with tolerations:

If you also want Pods on master/control-plane Nodes, you need to add tolerations in the Pod spec to allow scheduling on tainted Nodes.

key: node-role.kubernetes.io/control-plane
operator: Exists
effect: NoSchedule


Updated YAML:

apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nginx
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      tolerations:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80


Here, the toleration overrides the master Node's NoSchedule taint, allowing the Pod to run on it too.

Verify Pods Now Run on All Nodes
kubectl get all -o wide


Pods are now running on:

ip-172-31-34-191 (Master)

ip-172-31-3-215 (Worker)

ip-172-31-5-194 (Worker)

daemonset.apps/nginx → DESIRED: 3, CURRENT: 3, READY: 3
NAME
daemonset.apps/nginx

DESIRED CURRENT READY UP-TO-DATE AVAILABLE NODE SELECTOR AGE CONTAINERS
3 3 3 3 3 <none> 33s nginx

Now 3 Pods → one on each Node (Master + 2 Workers).

Without tolerations → DaemonSet Pods run only on Worker Nodes.

With tolerations → DaemonSet Pods run on all Nodes including Master.

7. Key Takeaways

DaemonSet = One Pod per Node, automatically managed by Kubernetes.

Best suited for system-level tasks like monitoring, logging, or networking.

Doesn’t run on master Nodes by default → you need tolerations to include them.

Unlike Deployments, you don’t set replicas manually; the Pod count always equals the Node count.
